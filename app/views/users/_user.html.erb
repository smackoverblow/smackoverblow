<li>
  <div class="adiv">
  	<table>
	  	<tr><%= gravatar_for user, size: 100%></tr>
	  	<tr class="username">
        <td align="center">
          <%= link_to user.name, user %> 
          <% if current_user.admin? && !current_user?(user) %>
            | <%= link_to "x", user, method: :delete, data: {confirm: "You sure?"} %>
          <% end %>
        </td>
      </tr>
  </table>
  </div>
</li>