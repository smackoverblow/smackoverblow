<%= form_for post, url: accept_answer_post_url, html: { method: :patch }, namespace: answer.id, remote: true do |f| %>
  <%= hidden_field_tag :answer_id, answer.id %>
  <%= f.submit 'Accept answer', class: 'btn btn-primary' %>
<% end %>